
<!--missing description to movies - add to householdmovies-->
<%= @movie.title %>

  <%= simple_form_for [@household, @movie] do |f| %>
  <%= f.input :title %>
  <%= f.input :genre %>
  <%= f.input :imdb_rating %>
  <%= f.submit %>
  <%= f.button :submit, class: 'btn btn-secondary' %>
 <% end %>

<!--household_household_movies -->
